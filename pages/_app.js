// Svg are the way to go
// en using tailwind css, we start with mobile first as it we were making it for mobile then adjust to big sizes

import "../styles/globals.css";

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;
